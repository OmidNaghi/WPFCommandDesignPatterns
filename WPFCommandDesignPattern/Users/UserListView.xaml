<UserControl x:Class="WPFCommandDesignPattern.Users.UserListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:users="clr-namespace:WPFCommandDesignPattern.Users"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <!--<UserControl.DataContext>
        <users:UseListViewModel></users:UseListViewModel>
    </UserControl.DataContext>-->
    <Grid>
        <DataGrid ItemsSource="{Binding Users}"></DataGrid>
    </Grid>
</UserControl>
